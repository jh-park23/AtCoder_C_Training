#include<stdio.h>

int check_num(const int *a, const int *b)
{
    if(*b % *a == 0)
    {
        return *a + *b;
    }
    else
    {
        return *b - *a; 
    }
}
int main()
{
    int A, B;
    scanf("%d %d", &A, &B);

    printf("%d", check_num(&A, &B));

    return 0;
}