#include<stdio.h>

void find_directly(const int *a, const int *b, const int *c, const int *d, const int *e, const int *k)
{
    if(*e-*a>*k)
    // because largest distance more than k => indireactly
    {
        printf(":(\n");
    }
    else
    {
        printf("Yay!\n");
    }
}

int main()
{
    int a, b, c, d, e, k;
    scanf("%d %d %d %d %d %d", &a, &b, &c, &d, &e, &k);

    find_directly(&a, &b, &c, &d, &e, &k);

    return 0;
}