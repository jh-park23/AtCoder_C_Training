#include<stdio.h>

void check_AC(const int N, const int M)
{
    if(N == M)
    {
        printf("Yes\n");
    }
    else
    {
        printf("No\n");
    }
}
int main()
{
    int N, M;
    scanf("%d %d", &N, &M);

    check_AC(N, M);

    return 0;
}