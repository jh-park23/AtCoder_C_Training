#include<stdio.h>

void compare(const int * a, const int *b, const int *c)
{
    if(*a>2019||(*a==2019 && *b>=5))
    {
        printf("TBD");
    }
    else
    {
        printf("Heisei");
    }
}

int main()
{
    char S[11];
    scanf("%s", S);
    
    int year, month, day;
    sscanf(S, "%4d/%2d/%2d", &year, &month, &day);

    compare(&year, &month, &day);

    return 0;
}