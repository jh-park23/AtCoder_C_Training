#include<stdio.h>

int main()
{
    char C[4][4];
    scanf("%s%s%s", C[0], C[1], C[2]);

    for(int i=0; i<3;i++)
    {
        for(int j=0; j<3; j++)
        {
            if(i==j)
            {
                printf("%c", C[i][j]);
            }
            else
            {
                continue;
            }
            
        }
    }

    return 0;
}