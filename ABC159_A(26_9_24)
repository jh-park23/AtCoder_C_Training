#include<stdio.h>

void count_even_pairs(const int N, const int M)
{
    int total_pairs = ((N*(N-1))) / 2 + ((M*(M-1)) / 2);

    printf("%d\n", total_pairs);
}

int main()
{
    int N, M;
    scanf("%d %d", &N, &M);

    count_even_pairs(N, M);

    return 0;
}