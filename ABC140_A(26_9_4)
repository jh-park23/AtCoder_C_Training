#include<stdio.h>

void possible_set(const int *a)
{
    int set_num = 1;

    for(int i = 0; i < 3; i++)
    {
        set_num *= *a;
    }

    printf("%d\n", set_num);
}

int main()
{
    int N;
    scanf("%d", &N);

    possible_set(&N);

    return 0;
}