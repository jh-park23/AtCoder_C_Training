#include<stdio.h>

int main()
{
    char S[4];
    scanf("%s", S);
    int count=0;

    for(int i = 0; i < 3; i++)
    {
        if(S[i]=='1')
        {
            count++;
        }
        else
        {
            continue;
        }
    }

    printf("%d", count);

    return 0;

}