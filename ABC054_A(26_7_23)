#include<stdio.h>

int main()
{
    int A, B;
    scanf("%d %d", &A, &B);

    if(A==1)
    {
        A=14;
    }
    if(B==1)
    {
        B=14;
    }

    if(A>B)
    {
        printf("Alice");
    }
    if(A==B)
    {
        printf("Draw");
    }
    if(A<B)
    {
        printf("Bob");
    }

    return 0;
}