#include<stdio.h>
#include<string.h>

void count_mid(const char *s)
{
    int len = strlen(s);

    printf("%c%d%c", s[0], len -2, s[len - 1]);
}


int main()
{
    char s[101];
    scanf("%s", s);

    count_mid(s);

    return 0;
}