#include<stdio.h>

int main()
{
    int a, b, m;
    scanf("%d %d", &a, &b);

    m = a*b;

    if((m%2)==1)
    {
        printf("Odd");
    }
    else if((m%2)==0)
    {
        printf("Even");
    }

    return 0;
}