#include<stdio.h>

int white_cell(const int *a, const int *b, const int *c, const int *d)
{
    return (*a - *c) * (*b - *d);    
}

int main()
{
    int H, W, h, w;
    scanf("%d %d", &H, &W);
    scanf("%d %d", &h, &w);

    printf("%d", white_cell(&H, &W, &h, &w));

    return 0;
}